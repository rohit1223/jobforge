# Suggested next topics (not yet generated)

Ranked matched/JD-relevant topics to generate next. Re-run Mode 1 for the next ~3,
or `add-topic <name>` for a specific one.

- Linux & Systems Fundamentals — processes, cgroups/namespaces, fds, signals; underpins your container & infra work
- Networking (TCP/TLS/HTTP) — load balancing, keep-alive, TLS handshake; matches your ALB/Route53/mTLS experience
- SQL & Data Modeling — indexing, query plans, transactions/isolation; you list SQL and ran RDS/Postgres-adjacent systems
- Caching & Redis — eviction, persistence, clustering; directly ties to your Redis state-tier live-migration
- System Design (general) — end-to-end design drills (rate limiter, feed, control plane) for senior loops
- Security Engineering — authn/authz, secrets, mTLS, least-privilege; matches your RBAC/mTLS/IAM work
- Behavioral / Leadership (STAR) — structured stories from your 15-team, cross-org impact
